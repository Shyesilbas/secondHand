This monolithic file is deprecated.

Architecture decisions are now decentralized for human readers in the `.agents/decisions/` directory, and for AI execution, they are embedded directly into the relevant Skills (`.agents/skills/`) and Domain READMEs (`src/domain/README.md`).
